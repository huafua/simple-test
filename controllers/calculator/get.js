export const add = ({ a, b }) => parseInt(a) + parseInt(b);

export const subtract = ({ a, b }) => parseInt(a) - parseInt(b);

export const multiply = ({ a, b }) => parseInt(a) * parseInt(b);

// export const divide = ({ a, b }) => parseInt(a) / parseInt(b);
